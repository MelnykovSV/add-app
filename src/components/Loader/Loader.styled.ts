import styled from '@emotion/styled';

export const Wrapper = styled.div`
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;

  z-index: 10;
  background-color: rgba(40, 49, 73, 0.3);
  display: flex;
  justify-content: center;
  align-items: center;
`;
